import tkinter as tk

# Initialize the main window
window = tk.Tk()
window.title("Mile to Km Converter")
window.minsize(width=500, height=300)
window.config(padx=20, pady=20)

# Entry widget for miles input
miles_input = tk.Entry(width=10)
miles_input.grid(column=1, row=0)

# Label for "Miles"
miles_label = tk.Label(text="Miles")
miles_label.grid(column=2, row=0)

# Label for "is equal to"
equal_label = tk.Label(text="is equal to")
equal_label.grid(column=0, row=1)

# Label for the result in km
result_label = tk.Label(text="0")
result_label.grid(column=1, row=1)

# Label for "Km"
km_label = tk.Label(text="Km")
km_label.grid(column=2, row=1)

# Function to convert miles to kilometers
def convert_miles_to_km():
    miles = miles_input.get()
    miles = float(miles)
    km = miles * 1.60934
    result_label.config(text=f"{km:.2f}")

# Button to trigger the conversion
calculate_button = tk.Button(text="Calculate", command=convert_miles_to_km)
calculate_button.grid(column=1, row=2)

# Start the Tkinter event loop
window.mainloop()

# # Extra code to learn about creating buttons
# # # Add a new button
# def new_button_clicked():
#     print("New button clicked")
#
#
# button2 = tk.Button(text="New Button", command=new_button_clicked)
# button2.grid(column=2, row=0)
